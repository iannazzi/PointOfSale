function replaceImage(location)
{
	if(confirm("Certain about that replace?"))
			{
				needToConfirm = false;
				open_win(location);
			}
}
function deleteImage(location)
{
	if(confirm("Certain about that delete?"))
			{
				needToConfirm = false;
				open_win(location);
			}
}